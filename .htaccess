# Force PHP 8.3 pour klassciMaster (compatible Laravel 12)
AddHandler application/x-httpd-ea-php83 .php

<IfModule mod_rewrite.c>
    RewriteEngine On

    # Redirect to public folder
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ /public/$1 [L]
</IfModule>
