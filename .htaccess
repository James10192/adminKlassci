# Redirect to public folder
# Note: PHP version configured via cPanel MultiPHP Manager (PHP 8.3)
<IfModule mod_rewrite.c>
    RewriteEngine On

    # Redirect to public folder
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ /public/$1 [L]
</IfModule>
